# Stackage LTS 24.57 — GHC 9.10
resolver: lts-24.57

packages:
- .

# the example renderer is behind a manual cabal flag so that the library on
# Hackage does not carry the image dependencies
flags:
  pure-cdt:
    examples: true
